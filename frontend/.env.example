# Google sign-in (optional)
VITE_GOOGLE_CLIENT_ID=

# PayPal checkout. Without this the store renders but cannot take payment.
# Must be the client ID for the same environment the backend is pointed at.
VITE_PAYPAL_CLIENT_ID=
